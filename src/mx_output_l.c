#include "uls.h"

// void mx_output_l(const char* file, int length, int max_size) {
// 	for( int i = 0; i < length; i++) {
// 		int link = mx_count_link(file);
// 		char* str;
// 		itoa(link, str, 10);
// 		int len = strlen(str);
//         if (len > max_size)
//             max_size = len;
//     }
//     for( int j = 0; j < length; j++) {
//     	int link = mx_count_link(file);
// 		char* str;
// 		itoa(link, str, 10);
// 		int len = mx_strlen(str);
// 		if (len < max_size) {
// 			for(int j = 0; j < max_size - len; j++) {
// 				str =  strcat(str, " ");
// 		}
// 		printf("  %s",str);
// 	}
// }
// }
